---
description: Start the IP-Sim development environment (Frontend Build + Backend Run)
---

This workflow executes the \`start-dev.sh\` script to build the frontend and launch the Spring Boot application.

1.  Run the dev script.
    // turbo
    \`\`\`bash
    ./start-dev.sh
    \`\`\`
